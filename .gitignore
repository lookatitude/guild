# Node
node_modules/
*.log

# Guild runtime state (project-local, never committed by Guild itself)
.guild/

# OS
.DS_Store

# Accidental root lockfile (scripts/, hooks/, mcp-servers/ have their own)
/package-lock.json
