# /guild
Step 1: procedure text that belongs in an assembler, not a command.
Step 2: procedure text that belongs in an assembler, not a command.
Step 3: procedure text that belongs in an assembler, not a command.
Step 4: procedure text that belongs in an assembler, not a command.
Step 5: procedure text that belongs in an assembler, not a command.
Step 6: procedure text that belongs in an assembler, not a command.
Step 7: procedure text that belongs in an assembler, not a command.
Step 8: procedure text that belongs in an assembler, not a command.
Step 9: procedure text that belongs in an assembler, not a command.
Step 10: procedure text that belongs in an assembler, not a command.
Step 11: procedure text that belongs in an assembler, not a command.
Step 12: procedure text that belongs in an assembler, not a command.
Step 13: procedure text that belongs in an assembler, not a command.
Step 14: procedure text that belongs in an assembler, not a command.
Step 15: procedure text that belongs in an assembler, not a command.
Step 16: procedure text that belongs in an assembler, not a command.
Step 17: procedure text that belongs in an assembler, not a command.
Step 18: procedure text that belongs in an assembler, not a command.
Step 19: procedure text that belongs in an assembler, not a command.
Step 20: procedure text that belongs in an assembler, not a command.
Step 21: procedure text that belongs in an assembler, not a command.
Step 22: procedure text that belongs in an assembler, not a command.
Step 23: procedure text that belongs in an assembler, not a command.
Step 24: procedure text that belongs in an assembler, not a command.
Step 25: procedure text that belongs in an assembler, not a command.
Step 26: procedure text that belongs in an assembler, not a command.
Step 27: procedure text that belongs in an assembler, not a command.
Step 28: procedure text that belongs in an assembler, not a command.
Step 29: procedure text that belongs in an assembler, not a command.
Step 30: procedure text that belongs in an assembler, not a command.
Step 31: procedure text that belongs in an assembler, not a command.
Step 32: procedure text that belongs in an assembler, not a command.
Step 33: procedure text that belongs in an assembler, not a command.
Step 34: procedure text that belongs in an assembler, not a command.
Step 35: procedure text that belongs in an assembler, not a command.
Step 36: procedure text that belongs in an assembler, not a command.
Step 37: procedure text that belongs in an assembler, not a command.
Step 38: procedure text that belongs in an assembler, not a command.
Step 39: procedure text that belongs in an assembler, not a command.
Step 40: procedure text that belongs in an assembler, not a command.
Step 41: procedure text that belongs in an assembler, not a command.
Step 42: procedure text that belongs in an assembler, not a command.
Step 43: procedure text that belongs in an assembler, not a command.
Step 44: procedure text that belongs in an assembler, not a command.
