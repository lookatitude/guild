{
  "schema": "guild.layout_baseline_additions.v1",
  "note": "PR-SCOPED declarations for entries ADDED to layout-baseline.json. Every added key must appear here with a reason and the owning lane, or baseline-shrink-gate fails. A key listed here but absent from the baseline is a stale declaration and also fails. This file must be EMPTY on next/main: declarations live only for the life of a PR, so the gate fails a push to a channel branch while it is non-empty.",
  "additions": [
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/context/workflows/recall-protect.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/context/workflows/recall-protect.ts writes under a Guild root and names the wiki without a scrubbedWrite call site. Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/dispatch/resources/scripts/agent-team-launcher.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/agent-team-launcher.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/distribution/resources/scripts/lib/skill-source-transform.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/lib/skill-source-transform.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/evolution/resources/scripts/lib/reaping.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/lib/reaping.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/evolution/resources/scripts/shadow-mode.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/shadow-mode.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/host-runtime/resources/scripts/lib/pane-adapter.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/lib/pane-adapter.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/host-runtime/resources/scripts/write-host-capability.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/write-host-capability.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/host-runtime/resources/src/modules/context/workflows/recall-protect.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/context/workflows/recall-protect.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/host-runtime/resources/src/modules/lifecycle/workflows/run-state.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/lifecycle/workflows/run-state.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/host-runtime/resources/src/modules/migrations/workflows/index-migrate.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/migrations/workflows/index-migrate.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/host-runtime/resources/src/modules/security/workflows/events.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/security/workflows/events.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/knowledge/resources/scripts/lib/kb-snapshot.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/lib/kb-snapshot.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/lifecycle/resources/src/modules/lifecycle/workflows/run-state.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/lifecycle/workflows/run-state.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/lifecycle/resources/src/modules/migrations/workflows/index-migrate.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/migrations/workflows/index-migrate.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/lifecycle/resources/src/modules/security/workflows/events.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/security/workflows/events.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/lifecycle/workflows/run-state.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/lifecycle/workflows/run-state.ts writes under a Guild root and names the wiki without a scrubbedWrite call site. Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/migrations/resources/scripts/dot-guild/audit.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/dot-guild/audit.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/migrations/resources/scripts/dot-guild/convert/index.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/dot-guild/convert/index.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/migrations/workflows/index-migrate.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/migrations/workflows/index-migrate.ts writes under a Guild root and names the wiki without a scrubbedWrite call site. Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/review/resources/scripts/flip-report.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/flip-report.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/security/workflows/events.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): src/modules/security/workflows/events.ts writes under a Guild root and names the wiki without a scrubbedWrite call site. Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/state/resources/scripts/lib/retention.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/lib/retention.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/telemetry/resources/scripts/trace-summarize.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing wiki writer surfaced by the raw-text fail-closed rule (codex G-lane r2): scripts/trace-summarize.ts writes under a Guild root and names the wiki without a scrubbedWrite call site (module-resource mirror of that file). Not introduced by T02; the old literal-only predicate could not see it. Clears when T09/U-LOOP routes the write through scrubbedWrite.",
      "lane": "T02"
    },
    {
      "key": "no-dispatched-claude-agents::skills/meta/execute-plan/references/context-assemble.md::shipped surface references .claude/agents",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-dispatched-claude-agents' violation now reports at 'skills/meta/execute-plan/references/context-assemble.md' instead of 'src/modules/context/resources/skills/meta/context-assemble/SKILL.md'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-dispatched-claude-agents::src/modules/lifecycle/resources/skills/meta/execute-plan/references/context-assemble.md::shipped surface references .claude/agents",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-dispatched-claude-agents' violation now reports at 'src/modules/lifecycle/resources/skills/meta/execute-plan/references/context-assemble.md' instead of 'src/modules/context/resources/skills/meta/context-assemble/SKILL.md'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/SKILL.md:40::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/SKILL.md:40' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/SKILL.md:66::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/SKILL.md:66' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/SKILL.md:76::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/SKILL.md:76' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/SKILL.md:92::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/SKILL.md:92' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/audit.md:40::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/audit.md:40' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.evals.json:7::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.evals.json:7' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.md:10::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.md:10' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.md:18::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.md:18' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.md:23::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.md:23' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.md:36::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.md:36' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.md:3::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.md:3' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.md:44::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.md:44' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::skills/meta/evolve/references/rollback-skill.md:68::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'skills/meta/evolve/references/rollback-skill.md:68' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/SKILL.md:40::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/SKILL.md:40' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/SKILL.md:66::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/SKILL.md:66' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/SKILL.md:76::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/SKILL.md:76' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/SKILL.md:92::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/SKILL.md:92' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/audit.md:40::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/audit.md:40' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:10::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:10' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:18::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:18' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:23::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:23' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:36::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:36' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:3::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:3' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:44::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:44' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-durable-skill-versions::src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:68::references a durable skill-versions/ tree",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-durable-skill-versions' violation now reports at 'src/modules/evolution/resources/skills/meta/evolve/references/rollback-skill.md:68' instead of 'skills/meta/rollback-skill/SKILL.md:44'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/ingest-reference.md:108::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/ingest-reference.md:108' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/ingest-reference.md:13::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/ingest-reference.md:13' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/ingest-reference.md:18::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/ingest-reference.md:18' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/ingest-reference.md:19::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/ingest-reference.md:19' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/ingest-reference.md:24::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/ingest-reference.md:24' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/ingest-reference.md:78::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/ingest-reference.md:78' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/ingest-reference.md:8::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/ingest-reference.md:8' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/lint-rules.md:35::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/lint-rules.md:35' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/query-modes.md:80::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/query-modes.md:80' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-ingest.md:12::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-ingest.md:12' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-ingest.md:18::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-ingest.md:18' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-ingest.md:23::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-ingest.md:23' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-ingest.md:32::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-ingest.md:32' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-ingest.md:3::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-ingest.md:3' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-lint.md:17::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-lint.md:17' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-lint.md:3::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-lint.md:3' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-lint.md:70::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-lint.md:70' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-query.md:12::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-query.md:12' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-query.md:23::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-query.md:23' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-query.md:3::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-query.md:3' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-query.md:80::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-query.md:80' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-query.md:82::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-query.md:82' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/knowledge/wiki/references/wiki-query.md:90::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/knowledge/wiki/references/wiki-query.md:90' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::skills/meta/init/SKILL.md:54::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'skills/meta/init/SKILL.md:54' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:108::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:108' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:13::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:13' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:18::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:18' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:19::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:19' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:24::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:24' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:78::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:78' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:8::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/ingest-reference.md:8' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/lint-rules.md:35::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/lint-rules.md:35' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/query-modes.md:80::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/query-modes.md:80' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:12::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:12' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:18::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:18' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:23::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:23' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:32::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:32' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:3::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-ingest.md:3' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-lint.md:17::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-lint.md:17' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-lint.md:3::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-lint.md:3' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-lint.md:70::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-lint.md:70' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:12::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:12' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:23::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:23' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:3::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:3' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:80::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:80' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:82::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:82' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:90::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/knowledge/resources/skills/knowledge/wiki/references/wiki-query.md:90' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-guild-raw-writes::src/modules/lifecycle/resources/skills/meta/init/SKILL.md:54::references .guild/raw",
      "reason": "Relocation, not a regression: T03 moved this file as part of the KTD59/KTD25 skill fold, so the SAME pre-existing 'no-guild-raw-writes' violation now reports at 'src/modules/lifecycle/resources/skills/meta/init/SKILL.md:54' instead of 'skills/knowledge/wiki-ingest/ingest-reference.md:24'. The old key is removed from the baseline in the same commit, so the entry count does not grow. Clears when the underlying violation is fixed by its owning lane, not by T03.",
      "lane": "T03"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-completed.ts:151::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-completed.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-completed.ts:171::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-completed.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-completed.ts:184::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-completed.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-completed.ts:342::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-completed.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-completed.ts:391::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-completed.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-completed.ts:672::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-completed.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-created.ts:195::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-created.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/task-created.ts:84::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/task-created.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/teammate-idle.ts:145::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/teammate-idle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/teammate-idle.ts:226::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/teammate-idle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/teammate-idle.ts:471::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/teammate-idle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/agent-team/teammate-idle.ts:547::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/agent-team/teammate-idle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/capture-telemetry.ts:302::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/capture-telemetry.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/detect-guild-version.ts:206::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/detect-guild-version.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/emit-learning-checkpoint.ts:344::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/emit-learning-checkpoint.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/emit-learning-checkpoint.ts:406::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/emit-learning-checkpoint.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/emit-learning-checkpoint.ts:439::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/emit-learning-checkpoint.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/emit-learning-checkpoint.ts:445::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/emit-learning-checkpoint.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/gate-outcome-writer.ts:150::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/gate-outcome-writer.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/gate-outcome-writer.ts:55::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/gate-outcome-writer.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/backend-degradation.ts:349::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/backend-degradation.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/backend-degradation.ts:455::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/backend-degradation.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/backend-degradation.ts:673::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/backend-degradation.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/guild-root.ts:88::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/guild-root.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/heartbeat-write.ts:179::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/heartbeat-write.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/heartbeat.ts:165::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/heartbeat.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/lean-lead-guard.ts:432::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/lean-lead-guard.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/learning-backstop.ts:155::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/learning-backstop.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/lifecycle-gate.ts:972::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/lifecycle-gate.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/reanchor.ts:208::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/reanchor.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/reanchor.ts:229::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/reanchor.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/reanchor.ts:360::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/reanchor.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/run-trace.ts:127::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/run-trace.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/run-trace.ts:128::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/run-trace.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/run-trace.ts:242::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/run-trace.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/run-trace.ts:932::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/run-trace.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/lib/tier-dispatch.ts:319::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/lib/tier-dispatch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/maybe-reflect.ts:204::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/maybe-reflect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/maybe-reflect.ts:376::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/maybe-reflect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/maybe-reflect.ts:434::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/maybe-reflect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/maybe-reflect.ts:458::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/maybe-reflect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/maybe-reflect.ts:568::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/maybe-reflect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/maybe-reflect.ts:600::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/maybe-reflect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/post-tool-use.ts:159::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/post-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/post-tool-use.ts:275::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/post-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/post-tool-use.ts:336::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/post-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/post-tool-use.ts:374::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/post-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/pre-compact.ts:112::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/pre-compact.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/pre-compact.ts:202::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/pre-compact.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/pre-tool-use.ts:1425::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/pre-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/pre-tool-use.ts:1469::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/pre-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/pre-tool-use.ts:172::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/pre-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/pre-tool-use.ts:243::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/pre-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/pre-tool-use.ts:656::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/pre-tool-use.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/run-trace-close.ts:410::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/run-trace-close.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/test-support/mint-binding.ts:32::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/test-support/mint-binding.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::hooks/update-check.ts:69::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in hooks/update-check.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::mcp-servers/guild-memory/src/index.ts:243::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in mcp-servers/guild-memory/src/index.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::mcp-servers/guild-memory/src/index.ts:259::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in mcp-servers/guild-memory/src/index.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::mcp-servers/guild-telemetry/src/index.ts:252::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in mcp-servers/guild-telemetry/src/index.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::mcp-servers/guild-telemetry/src/index.ts:261::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in mcp-servers/guild-telemetry/src/index.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::mcp-servers/guild-telemetry/src/index.ts:271::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in mcp-servers/guild-telemetry/src/index.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:1533::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:1857::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:2022::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:2188::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:3194::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:3320::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:3458::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:921::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/agent-team-launcher.ts:971::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/agent-team-launcher.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/analyze-runs.ts:201::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/analyze-runs.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/analyze-runs.ts:245::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/analyze-runs.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/analyze-runs.ts:539::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/analyze-runs.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/audit-run-sinks.ts:45::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/audit-run-sinks.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/check-roster-consistency.ts:251::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/check-roster-consistency.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:1076::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:2250::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:2251::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:2256::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:2257::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:2591::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:2587::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/config-cmd.ts:2589::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/config-cmd.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:324::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:335::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:356::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:360::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:465::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:478::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:562::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:577::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:709::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/dashboard-launch.ts:888::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/dashboard-launch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/description-optimizer.ts:138::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/description-optimizer.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/description-optimizer.ts:283::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/description-optimizer.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/evolve-loop.ts:112::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/evolve-loop.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/evolve-loop.ts:295::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/evolve-loop.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/evolve-loop.ts:303::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/evolve-loop.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/evolve-loop.ts:310::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/evolve-loop.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/feedback-triage.ts:84::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/feedback-triage.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/flip-report.ts:321::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/flip-report.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/flip-report.ts:322::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/flip-report.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/host-smoke.ts:455::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/host-smoke.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/ideation-min-build-cli.ts:48::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/ideation-min-build-cli.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/ideation-min-build-cli.ts:49::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/ideation-min-build-cli.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/initiative-gate.ts:450::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/initiative-gate.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/initiative-gate.ts:477::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/initiative-gate.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/initiative-gate.ts:96::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/initiative-gate.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/instantiate-template.ts:245::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/instantiate-template.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:286::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:343::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:381::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:492::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:643::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:680::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:718::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-builder.ts:836::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-builder.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/knowledge-links-traverse.ts:238::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/knowledge-links-traverse.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/derive-domain.ts:23::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/derive-domain.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/diff-learn.ts:25::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/diff-learn.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/k-stage-staleness.ts:453::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/k-stage-staleness.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/k-stage-staleness.ts:490::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/k-stage-staleness.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/k-stage-staleness.ts:525::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/k-stage-staleness.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/k-stage-staleness.ts:601::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/k-stage-staleness.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/knowledge-orchestrator.ts:1118::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/knowledge-orchestrator.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/knowledge-orchestrator.ts:1191::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/knowledge-orchestrator.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/knowledge-orchestrator.ts:1192::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/knowledge-orchestrator.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/knowledge-orchestrator.ts:1213::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/knowledge-orchestrator.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/knowledge-orchestrator.ts:1373::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/knowledge-orchestrator.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/knowledge-orchestrator.ts:733::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/knowledge-orchestrator.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/knowledge-orchestrator.ts:931::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/knowledge-orchestrator.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/lib/paths.ts:85::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/lib/paths.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/learn/write-knowledge-links.ts:230::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/learn/write-knowledge-links.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/benchmark-guild-artifacts.ts:37::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/benchmark-guild-artifacts.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/benchmark-guild-artifacts.ts:59::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/benchmark-guild-artifacts.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/adoption-migrate.ts:676::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/adoption-migrate.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/compatibility-loader.ts:155::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/compatibility-loader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/compatibility-loader.ts:245::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/compatibility-loader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/definition-ref-for-dispatch.ts:31::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/definition-ref-for-dispatch.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/migration-evidence.ts:2162::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/migration-evidence.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/migration-evidence.ts:2163::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/migration-evidence.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/migration-evidence.ts:417::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/migration-evidence.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/migration-evidence.ts:826::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/migration-evidence.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/self-build-canonicalize.ts:585::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/self-build-canonicalize.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/self-build-canonicalize.ts:642::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/self-build-canonicalize.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/capability/workspace-role-localize.ts:213::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/capability/workspace-role-localize.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/cloud-task-packet.ts:618::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/cloud-task-packet.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/config-reconcile.ts:100::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/config-reconcile.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/config-reconcile.ts:103::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/config-reconcile.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/core/config-cli.ts:1623::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/core/config-cli.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/core/config-cli.ts:2038::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/core/config-cli.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/core/config-cli.ts:953::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/core/config-cli.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/emit-readback-degradation.ts:84::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/emit-readback-degradation.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/emit-remote-orphan.ts:83::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/emit-remote-orphan.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/host-adapters/claude-code-cli.ts:79::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/host-adapters/claude-code-cli.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/host-adapters/wrapped-cli-base.ts:158::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/host-adapters/wrapped-cli-base.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/host/pane-dispatch-trace.ts:153::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/host/pane-dispatch-trace.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/ingest-similarity.ts:114::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/ingest-similarity.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/ingest-similarity.ts:177::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/ingest-similarity.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/reaping.ts:754::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/reaping.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/reaping.ts:767::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/reaping.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/retention.ts:81::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/retention.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:1052::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:277::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:349::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:450::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:528::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:649::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:665::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:838::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:875::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/roster.ts:946::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/roster.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/task-cell-launch-plan.ts:106::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/task-cell-launch-plan.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/update-check.ts:454::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/update-check.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/workspace-project-root.ts:12::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/workspace-project-root.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/lib/workspace-project-root.ts:32::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/lib/workspace-project-root.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/recall-stats.ts:377::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/recall-stats.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/registry-rollup.ts:266::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/registry-rollup.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/resolve-specialist-capability-scope.ts:94::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/resolve-specialist-capability-scope.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/rollback-walker.ts:161::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/rollback-walker.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/shadow-mode.ts:176::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/shadow-mode.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/shadow-mode.ts:360::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/shadow-mode.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/shadow-mode.ts:369::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/shadow-mode.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/stamp-recall-importance.ts:118::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/stamp-recall-importance.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/sync-migration.ts:48::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/sync-migration.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/task-cell-host-conformance.ts:58::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/task-cell-host-conformance.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/trace-summarize.ts:789::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/trace-summarize.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/validate-run-record.ts:70::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/validate-run-record.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::scripts/write-host-capability.ts:133::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in scripts/write-host-capability.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/capability/workflows/independence-record.ts:289::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/capability/workflows/independence-record.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/capability/workflows/inspection-persist.ts:82::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/capability/workflows/inspection-persist.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/capability/workflows/models-command.ts:198::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/capability/workflows/models-command.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/capability/workflows/models-command.ts:355::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/capability/workflows/models-command.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/capability/workflows/policy-migration.ts:180::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/capability/workflows/policy-migration.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/capability/workflows/routing-rollout.ts:198::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/capability/workflows/routing-rollout.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/capability/workflows/routing-rollout.ts:236::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/capability/workflows/routing-rollout.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/host-open-preflight.ts:144::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/host-open-preflight.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/host-open-preflight.ts:205::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/host-open-preflight.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/settings-reader.ts:1034::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/settings-reader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/settings-reader.ts:1066::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/settings-reader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/settings-reader.ts:1177::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/settings-reader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/settings-reader.ts:1232::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/settings-reader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/settings-resolver.ts:44::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/settings-resolver.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/config/workflows/workspace-manifest.ts:100::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/config/workflows/workspace-manifest.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/fs-scanner.ts:179::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/fs-scanner.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/protect-chunks-cli.ts:33::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/protect-chunks-cli.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/recall.ts:412::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/recall.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/recall.ts:534::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/recall.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/recall.ts:719::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/recall.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/recall.ts:821::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/recall.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/recall.ts:831::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/recall.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/recall.ts:889::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/recall.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/context/workflows/wiki-recall.ts:379::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/context/workflows/wiki-recall.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/confirmation-gate.ts:69::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/confirmation-gate.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/shadow-routing.ts:184::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/shadow-routing.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/task-assignment-v2.ts:654::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/task-assignment-v2.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/task-assignment-v2.ts:662::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/task-assignment-v2.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/task-cell-acceptance.ts:663::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/task-cell-acceptance.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/task-cell-acceptance.ts:879::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/task-cell-acceptance.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/task-cell-artifact-join.ts:64::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/task-cell-artifact-join.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/task-cell-runtime.ts:563::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/task-cell-runtime.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/dispatch/workflows/task-cell-telemetry-reconcile.ts:48::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/dispatch/workflows/task-cell-telemetry-reconcile.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/docs-sync/workflows/wiki-lint-checks.ts:146::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/docs-sync/workflows/wiki-lint-checks.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/docs-sync/workflows/wiki-lint-checks.ts:205::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/docs-sync/workflows/wiki-lint-checks.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/docs-sync/workflows/wiki-lint-checks.ts:79::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/docs-sync/workflows/wiki-lint-checks.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/host-runtime/workflows/session-context.ts:279::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/host-runtime/workflows/session-context.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/host-runtime/workflows/session-context.ts:306::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/host-runtime/workflows/session-context.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/learning/workflows/graph-query.ts:582::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/learning/workflows/graph-query.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/emit-loop-event.ts:202::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/emit-loop-event.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/emit-loop-event.ts:96::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/emit-loop-event.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-binding.ts:104::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-binding.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-binding.ts:117::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-binding.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-binding.ts:201::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-binding.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-binding.ts:209::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-binding.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-binding.ts:455::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-binding.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-binding.ts:456::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-binding.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1154::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1483::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1501::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1382::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1585::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1586::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1648::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:1689::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:353::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-lifecycle.ts:379::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-lifecycle.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API. (T06 rework-r2: line shifted by the U-CFG edits in this file; same site, same worklist.)",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-record-validate.ts:348::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-record-validate.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/run-state.ts:446::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/run-state.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/runstart-preflight.ts:721::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/runstart-preflight.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/runstart-preflight.ts:725::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/runstart-preflight.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/write-run-manifest.ts:86::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/write-run-manifest.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/write-task-run.ts:216::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/write-task-run.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/lifecycle/workflows/write-task-run.ts:349::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/lifecycle/workflows/write-task-run.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/security/workflows/config.ts:213::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/security/workflows/config.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/security/workflows/config.ts:267::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/security/workflows/config.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/security/workflows/config.ts:308::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/security/workflows/config.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/security/workflows/events.ts:285::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/security/workflows/events.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/dependency-graph-reader.ts:144::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/dependency-graph-reader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/dependency-graph-reader.ts:168::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/dependency-graph-reader.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/index-cache.ts:217::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/index-cache.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/index-cache.ts:218::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/index-cache.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/index-cache.ts:360::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/index-cache.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/index-cache.ts:361::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/index-cache.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/index-cache.ts:462::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/index-cache.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/state/workflows/index-cache.ts:463::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/state/workflows/index-cache.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/teams/workflows/team-file.ts:142::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/teams/workflows/team-file.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/teams/workflows/team-file.ts:169::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/teams/workflows/team-file.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/teams/workflows/team-file.ts:277::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/teams/workflows/team-file.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/teams/workflows/team-file.ts:37::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/teams/workflows/team-file.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1552::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1553::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1602::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1604::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:162::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1651::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1782::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1810::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1811::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1812::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1843::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:1844::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:314::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:588::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:679::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:701::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:705::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:706::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:777::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:841::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:909::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:987::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/run-analysis.ts:988::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/run-analysis.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/telemetry/workflows/task-cell-telemetry.ts:297::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/telemetry/workflows/task-cell-telemetry.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/detect.ts:140::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/detect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/detect.ts:146::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/detect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/detect.ts:147::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/detect.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/federated-query.ts:181::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/federated-query.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/federated-query.ts:183::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/federated-query.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/federated-query.ts:95::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/federated-query.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/promote-upstream.ts:153::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/promote-upstream.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/promote-upstream.ts:173::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/promote-upstream.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/promote-upstream.ts:363::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/promote-upstream.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "no-direct-guild-join::src/modules/workspace/workflows/write-manifest.ts:120::builds a .guild path directly; use GuildStorage (state storage is the only constructor)",
      "reason": "Pre-existing .guild path construction in src/modules/workspace/workflows/write-manifest.ts, surfaced by the new no-direct-guild-join check (KTD15) that T05 lands. Not introduced by T05: the rule did not exist before this lane. The consumer migration onto GuildStorage is T12's scope (U-STOR must not move consumer files first); this entry clears when that file takes its base from the storage API.",
      "lane": "T05"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/config/workflows/policy-resolver.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing no-direct-guild-join site whose line moved when T06 rework-r2 refused `config role`, wired the overlay into `show --sources` and deleted the dead settings.json writers. Not a new .guild join.",
      "lane": "T06"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/host-runtime/resources/src/modules/config/workflows/policy-resolver.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing no-direct-guild-join site whose line moved when T06 rework-r2 refused `config role`, wired the overlay into `show --sources` and deleted the dead settings.json writers. Not a new .guild join.",
      "lane": "T06"
    },
    {
      "key": "harvest-writer-calls-scrubbed-write::src/modules/lifecycle/resources/src/modules/config/workflows/policy-resolver.ts::writes the wiki without a call to scrubbedWrite",
      "reason": "Pre-existing no-direct-guild-join site whose line moved when T06 rework-r2 refused `config role`, wired the overlay into `show --sources` and deleted the dead settings.json writers. Not a new .guild join.",
      "lane": "T06"
    },
    {
      "key": "no-ts-workflows-dir::src/modules/lifecycle/resources/src/modules/prompting/workflows::workflows/ directory name is retired",
      "reason": "Pre-existing no-direct-guild-join site whose line moved when T06 rework-r2 refused `config role`, wired the overlay into `show --sources` and deleted the dead settings.json writers. Not a new .guild join.",
      "lane": "T06"
    }
  ]
}
