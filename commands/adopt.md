---
name: adopt
description: "Print-only alias (KTD14) — /guild:adopt is now `/guild:init adopt`. Prints the new spelling and exits; dispatches nothing, writes nothing."
argument-hint: ""
allowed-tools: Read
---

Print exactly this line, then stop. Invoke no skill, run no command, write no file:

/guild:adopt is now /guild:init adopt — re-run it that way.
