---
name: dashboard
description: "Print-only alias (KTD14) — /guild:dashboard is now `/guild:status dashboard`. Prints the new spelling and exits; dispatches nothing, writes nothing."
argument-hint: ""
allowed-tools: Read
---

Print exactly this line, then stop. Invoke no skill, run no command, write no file:

/guild:dashboard is now /guild:status dashboard — re-run it that way.
