---
name: resume
description: "Print-only alias (KTD14) — /guild:resume is now `/guild:status resume`. Prints the new spelling and exits; dispatches nothing, writes nothing."
argument-hint: ""
allowed-tools: Read
---

Print exactly this line, then stop. Invoke no skill, run no command, write no file:

/guild:resume is now /guild:status resume — re-run it that way.
