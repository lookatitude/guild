---
name: migrate
description: "Print-only alias (KTD14) — /guild:migrate is now `/guild:config migrate`. Prints the new spelling and exits; dispatches nothing, writes nothing."
argument-hint: ""
allowed-tools: Read
---

Print exactly this line, then stop. Invoke no skill, run no command, write no file:

/guild:migrate is now /guild:config migrate — re-run it that way.
