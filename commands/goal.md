---
name: goal
description: "Print-only alias (KTD14) — /guild:goal is now `/guild:plan goal`. Prints the new spelling and exits; dispatches nothing, writes nothing."
argument-hint: ""
allowed-tools: Read
---

Print exactly this line, then stop. Invoke no skill, run no command, write no file:

/guild:goal is now /guild:plan goal — re-run it that way.
