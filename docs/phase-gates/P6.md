# P6 Phase Gate

Date: 2026-04-24
Status: passed-with-deferrals

## Deliverables verified (contract level)

### 4 P6 skills

```
OK — 4 P6 skills pass invariants
```

### 5 P6 scripts + CLI convention

```
OK — 5 P6 scripts + shared CLI README present
```

### 4 P6 commands

```
OK — 4 P6 commands present and registered in plugin.json
```

### Cross-cutting test fixtures

```
OK — cross-cutting evolve + shadow test fixtures present
```

### Full test suite (130 tests expected)

```
hooks/:
Test Suites: 5 passed, 5 total
Tests:       31 passed, 31 total
Snapshots:   0 total
Time:        36.599 s

scripts/:
Test Suites: 6 passed, 6 total
Tests:       67 passed, 67 total
Snapshots:   0 total
Time:        60.623 s

tests/:
Test Suites: 2 passed, 2 total
Tests:       32 passed, 32 total
Snapshots:   0 total
Time:        27.239 s, estimated 36 s
```

### End-to-end evolve smoke

```
[flip-report] wrote flip report for run test-run → /tmp/guild-p6-smoke/.guild/evolve/test-run/flip-report.md
exit: 0
---
run_id: test-run
n_cases_current: 10
n_cases_proposed: 10
regressions: 0
fixes: 5
stable_pass: 5
stable_fail: 0
missing_pairs: 0
pass_rate_current: 0.500
pass_rate_proposed: 1.000
pass_rate_delta: +100.0%
duration_ms_current_mean: 1189.0
duration_ms_proposed_mean: 1162.0
duration_ms_delta: -2.3%
```

### Non-destructive discipline

```
OK — no P6 code writes to .guild/wiki/
```

Note: scripts/ and hooks/ contain zero references to `.guild/wiki/`. The one match found in
`skills/meta/audit/SKILL.md:40` is in an enumeration of *allowed* guild paths used to flag
`writes_outside_guild` — a documentation assertion, not a write operation.

### Regression check

```
OK — 67 skills (T2=13) + 13 specialists + 7 commands
```

### UserPromptSubmit dual-handler (B1 fix)

```
OK — UserPromptSubmit wires both handlers: 2 total
```

## Deferred from runtime

Plan §14 P6 deliverable ("promote one skill update + create one proposed specialist through shadow mode and boundary gates") is a RUNTIME test requiring a live Claude Code session, real task history producing reflections, and the automatic threshold triggering. P6 ships the CONTRACT (skills + scripts + commands + tests + non-destructive discipline). Live promotion + specialist creation deferred to consuming-repo adoption.

Also deferred (intentional scope reduction): MCP servers per §13.3 (`guild-memory`, `guild-telemetry`) — explicitly optional per the plan. Tracked as P7 followup.

## Review history

Mid-phase group review returned APPROVED_WITH_FOLLOWUPS with:
- 1 blocker (B1): shadow-mode read UserPromptSubmit events that capture-telemetry never wrote. Fixed by extending capture-telemetry + dual-wiring UserPromptSubmit.
- 3 important: guild-evolve.md path shape (fixed), evolve-skill artifact names (fixed), audit skill enumeration (fixed).
- 5 nits (tracked as P7 followups).

Fix commits: aea51be.

## Followups into P7+

- MCP servers (§13.3): mcp-servers/guild-memory/ (BM25 wiki search, triggered at 200+ pages) + mcp-servers/guild-telemetry/ (structured trace query). Optional per plan; defer until a consuming repo's wiki actually hits the scale threshold.
- Runtime dogfood: live evolve + create-specialist runs on a consuming repo's real task history.
- guild-stats to prefer structured JSON sidecars if/when flip-report.ts emits them (currently parses YAML frontmatter).
- Eval bootstrap from reflections (§11.2 step 2 fallback) — no test fixture today.
- shadow-mode divergence heuristic hardening (string-include on specialist name is coincidence-prone).
- rollback-walker no-mutation test (currently enforced by code review, not test).

Closed: 2026-04-24
