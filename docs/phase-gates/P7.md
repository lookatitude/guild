# P7 Phase Gate — v1 release gate

Date: 2026-04-24
Status: passed

## Deliverables verified

### Agent-team tmux launcher

```
OK — agent-team launcher + fixtures + safety guards present
```

### MCP servers

```
OK — 2 MCP servers + .mcp.json wired
```

### Plugin docs + README

```
OK — 5 plugin docs + README polish present
```

### Bootstrap banner (v1-accurate)

```
┌─────────────────────────────────────────────────────────────────┐
│  Guild 1.0.0 — self-evolving specialist teams for Claude Code   │
├─────────────────────────────────────────────────────────────────┤
│  Commands                                                       │
│                                                                 │
│    /guild              Full workflow: brainstorm → team →       │
│                        plan → execute → review → reflect        │
│    /guild:team         Compose or inspect the specialist team   │
│    /guild:wiki         Manage the project knowledge wiki        │
│    /guild:evolve       Skill self-improvement pipeline          │
│    /guild:rollback     Roll back a skill to a prior version     │
│    /guild:stats        Task + telemetry summary                 │
│    /guild:audit        Security audit of plugin scripts         │
│                                                                 │
│  Optional MCP servers (run 'npm install' in each folder to use):│
│    mcp-servers/guild-memory/      BM25 wiki search              │
│    mcp-servers/guild-telemetry/   Trace query over .guild/runs/ │
├─────────────────────────────────────────────────────────────────┤
│  Plan & architecture: guild-plan.md (start at §1 or §13.2)     │
│  Docs: docs/architecture.md, docs/specialist-roster.md          │
└─────────────────────────────────────────────────────────────────┘
```

### E2E dogfood trail

```
OK — dogfood lifecycle artifacts present under docs/phase-gates/dogfood/
```

### Full test suite

```
hooks/:
Tests:       31 passed, 31 total
Snapshots:   0 total
Time:        14.389 s

scripts/:
Tests:       76 passed, 76 total
Snapshots:   0 total
Time:        28.078 s

tests/:
Tests:       32 passed, 32 total
Snapshots:   0 total
Time:        19.359 s, estimated 27 s

mcp-servers/guild-memory/:
Tests:       13 passed, 13 total
Snapshots:   0 total
Time:        16.985 s, estimated 18 s

mcp-servers/guild-telemetry/:
Tests:       13 passed, 13 total
Snapshots:   0 total
Time:        17.084 s, estimated 18 s
```

### §14 phase-row coverage

```
OK — all 21 §14 phase-row checks pass
```

### Plugin version

```
OK — version: 1.0.0
```

## Release notes — v1.0.0

Guild v1 ships:
- 13 shipping specialists (engineering + content + commercial)
- 67 skills (1 T1 core, 13 T2 meta, 3 T3 knowledge, 50 T5 specialist)
- 7 slash commands
- 8 Claude Code hook events wired
- 7 tooling scripts + agent-team tmux launcher
- 2 optional stdio MCP servers
- 5 user-facing docs + README + single source-of-truth plan

## v1.1 targets (deferred from review)

- Run-id convention unification across launcher/hooks/telemetry
- team.yaml schema reconciliation (team-compose skill vs launcher vs dogfood)
- Agent-team launcher orchestrator prompt enrichment (explicit bundle path)
- Dep-ID regex expansion in task-created.ts to handle markdown-table plans
- Live /guild end-to-end dogfood against a consuming repo
- MCP server node_modules bundling (optional — current postinstall doc-path works)
- skills/fallback/ population (currently empty by design per §5 REFERENCE policy)

## Review history (P7)

Final v1 code review returned APPROVED_WITH_FOLLOWUPS with 2 blockers (bootstrap
banner stale labels; MCP deps undeclared on fresh install). Both fixed in
commit 72a55ba. The 5 important + 5 nit items are scoped to v1.1.
