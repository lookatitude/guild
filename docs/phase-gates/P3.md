# P3 Phase Gate

Date: 2026-04-24
Status: passed-with-deferrals

Initial gate pass (plugin-architect dispatch, agent `a7606e549774c279d`) flagged `agents/copywriter.md` description at 1049 chars vs. the 1024 cap — invariant regression from the Task 2 + Task 3 adjacent-boundary updates compounding on the file. Main session remediated in place (trimmed description to 841 chars preserving all 5 adjacent-specialist references and the full TRIGGER / DO NOT TRIGGER structure), then re-ran check #1 — now OK.

## Deliverables verified (contract level)

### 13 Shipping specialists

Invariant check — all 13 expected specialist files exist, frontmatter parses, description ≤ 1024 chars, TRIGGER + DO NOT TRIGGER present, body contains `## Skills pulled`, `## When to invoke`, `## Scope boundaries`:

```
OK — 13 specialists present and invariant-clean
```

Description lengths (all now ≤ 1024):

```
agents/architect.md: 955
agents/backend.md: 997
agents/copywriter.md: 841
agents/devops.md: 917
agents/marketing.md: 987
agents/mobile.md: 996
agents/qa.md: 968
agents/researcher.md: 888
agents/sales.md: 997
agents/security.md: 1001
agents/seo.md: 947
agents/social-media.md: 864
agents/technical-writer.md: 963
```

### 50 T5 specialist skills

Invariant check — 50 T5 skills exist, frontmatter parses, `type: specialist`, description ≤ 1024 chars, TRIGGER + DO NOT TRIGGER present, each `evals.json` has ≥ 3 `should_trigger` and ≥ 3 `should_not_trigger`:

```
OK — 50 T5 specialist skills pass invariants
```

### Boundary-collision evals

`tests/boundary/evals.json` exists, tier is `boundary`, ≥ 40 cases, and each of the 12 collision axes has ≥ 3 cases:

```
OK — 61 boundary cases, all 12 axes covered
```

### Forward-reference resolution

Every `skills:` frontmatter entry across all 13 specialists resolves to a SKILL.md on disk:

```
OK — every specialist's skills: entries resolve to real skill dirs
```

### Full skill tally

1 T1 + 8 T2 + 3 T3 + 50 T5 = 62 skills total:

```
T1=1 T2=8 T3=3 T5=50 total=62
```

### Plugin manifest integrity

`.claude-plugin/plugin.json` parses as valid JSON:

```
OK
```

## Deferred from runtime

Plan §14 P3 speaks of "/guild:team propose picks correct teams across 25 synthetic specs" — that is a RUNTIME test requiring the team-compose skill + paired-eval harness (tooling-engineer's P6 work). P3 ships the contract (specialists, skills, boundary evals). Live runtime verification of team-compose accuracy is deferred.

## Review history

Mid-phase group review on the 13-specialist roster caught 2 important DO NOT TRIGGER completeness gaps (qa.md + devops.md omitting researcher). Both fixed in commit 8aa2f6a before T5 skills landed. The value of that mid-phase review is why it's worth keeping at every phase boundary — but note the review did not catch the 1049-char description on copywriter.md, which means future phases should add an automated invariant check to the review pass (not just human eyes).

## Followups into P4+

- **Add automated invariant pre-flight to mid-phase reviews** — group review missed the copywriter description-length breach. Candidate for a small script that runs the same invariants the gate runs, available to any mid-phase review.
- **Boundary eval runner** — `tests/boundary/evals.json` has 61 cases but no runner yet. Candidate for P6 tooling-engineer paired-eval harness.
- **Team-compose runtime verification** — §14 P3 deliverable ("25 synthetic specs routed correctly") remains deferred until team-compose can execute + paired-eval harness lands.
- **Invariant enforcement in CI** — currently only enforced at phase gates. Candidate for P4 hook work (pre-commit or on-push) so drift is caught at commit time.

Closed: 2026-04-24
