# P4 Phase Gate

Date: 2026-04-24
Status: passed-with-deferrals

## Deliverables verified (contract level)

### Agent-team hook manifest + handlers

```
OK — hook handlers, fixtures, hooks.json manifest present
```

### Experimental opt-in gate

```
OK hooks/agent-team/task-created.ts
OK hooks/agent-team/task-completed.ts
OK hooks/agent-team/teammate-idle.ts
```

### Hook handler tests

```
Test Suites: 3 passed, 3 total
Tests:       14 passed, 14 total
Snapshots:   0 total
Time:        10.685 s, estimated 12 s
```

### Hook handler smoke tests

```
[task-created] OK: task "task-001" owned by "backend" passed all validations.
exit: 0
[task-created] BLOCKED: Task "task-002" has no owner specialist assigned (teammate_name is empty). Assign a specialist before queueing this task.
exit: 1
[teammate-idle] INFO: teammate="backend" assigned=[] completed=[] pending=[] activeLog=false
[TeammateIdle 2026-04-24T17:45:37.382Z] Teammate "backend" (team: "guild-team") is idle.
If you have an active task, please write a handoff receipt or update your in-progress log to signal activity. Receipt path: /Users/example/Projects/guild/.guild/runs/run-sess-abc123/handoffs/backend-<task-id>.md
Required sections: changed_files, opens_for, assumptions, evidence, followups.
If all tasks are complete, no action is needed.
exit: 0
exit: 0
```

Expected exits: 0, non-zero (1), 0, 0 — all match spec.

### /guild:team slash command

```
OK — /guild:team wires skill, documents --allow-larger flag and cap-6 rule
```

### Plugin manifest integrity

```
OK — plugin.json registers 7 commands (includes guild-team)
```

### Regression check

```
OK — 62 skills + 13 specialists (no regression)
```

## Deferred from runtime

Plan §14 P4 deliverable ("agent-team run succeeds on a cross-layer task") requires a live Claude Code session with CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 and a consuming repo. Main session verified CONTRACT-level readiness: handlers enforce §13.2 gates, fixtures exercise valid/invalid paths, Jest suite is green. Runtime cross-layer run is deferred to first consuming-repo adoption.

## Review history

Mid-phase group review returned APPROVED_WITH_FOLLOWUPS with 2 important doc/impl mismatches (teammate-idle hasActiveLog not gating; task-created depends-on check is payload-text-only). Fixed by narrowing docstrings to describe actual behavior and flagging the stronger-enforcement versions as P5/P6 refinements. Commit 72c3012.

## Followups into P5+

- Strengthen task-created depends-on check: parse plan-task blocks by task_id and verify their dependencies (currently payload-text-regex only).
- Add hasActiveLog gating to teammate-idle nudge logic once P5 telemetry provides real freshness signals.
- Wire remaining 5 events in hooks.json (SessionStart, UserPromptSubmit, PostToolUse, SubagentStop, Stop) — P5 scope.
- Runtime dogfood once a consuming repo enables agent-team backend.
- hooks.json _comment_* field may flag JSON-schema validators — consider moving P5 event documentation to hooks/README.md.

Closed: 2026-04-24
