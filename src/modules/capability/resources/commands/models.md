---
name: models
description: "Print-only alias (KTD14) — /guild:models is now `/guild:config models`. Prints the new spelling and exits; dispatches nothing, writes nothing."
argument-hint: ""
allowed-tools: Read
---

Print exactly this line, then stop. Invoke no skill, run no command, write no file:

/guild:models is now /guild:config models — re-run it that way.
