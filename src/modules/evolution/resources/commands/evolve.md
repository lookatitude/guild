---
name: evolve
description: "Print-only alias (KTD14) — /guild:evolve is now `/guild:maintain evolve`. Prints the new spelling and exits; dispatches nothing, writes nothing."
argument-hint: ""
allowed-tools: Read
---

Print exactly this line, then stop. Invoke no skill, run no command, write no file:

/guild:evolve is now /guild:maintain evolve — re-run it that way.
